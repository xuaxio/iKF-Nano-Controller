# iKF-Nano 电脑端控制软件

蓝牙降噪耳机（iKF-Nano 系）的 Windows 图形化控制工具。逆向官方手机 App 的私有蓝牙协议实现，无需手机即可在电脑上完整控制耳机。

![图标](ikf_icon.ico)

## 功能

| 功能 | 说明 |
|---|---|
| 降噪控制 | 正常 / 通透 / 降噪三种模式，降噪下可选 自适应 / 轻度 / 均匀 / 重度 四档强度 |
| LDAC 开关 | 一键切换 LDAC 高音质模式 |
| 状态显示 | 实时显示当前降噪状态、LDAC 状态、电量（与官方 App 同一数据源） |
| 设置记忆 | 像手机 App 一样记住上次的降噪档位与 LDAC 设置，重连/重启后自动恢复 |
| 自动重连 | 检测到蓝牙断连后自动重新连接，并恢复记忆设置（LDAC 切换导致的短时断连可自动恢复） |
| 后台驻留 | 关闭窗口最小化到系统托盘，随时恢复 |
| 开机自启 | 可选开机静默后台运行，自动连接耳机并恢复设置 |

## 使用

```
python ikf_app.py                    # 打开图形界面
python ikf_app.py --autostart        # 静默后台运行
python ikf_control.py status         # 命令行读状态
python ikf_control.py anc deep       # 命令行切降噪-重度
python ikf_control.py ldac on        # 命令行开 LDAC
```

图形界面下直接点选即可，发送后自动读回确认。

## 协议说明

通过解析官方 App 的蓝牙 HCI 日志逆向得到，非公开协议：

- **通道**：BLE GATT，服务 `0x00FE`，写特征 `0x00F1`（发指令），通知特征 `0x00F2`（收回执）
- **帧格式**：`FF <序列号> <长度> <命令+载荷> AA`（无校验位）
- **命令字**：
  - 降噪：`09 <模式> <档位>` — 模式 01=降噪 02=正常 03=通透；档位 00=自适应 01=轻度 02=均匀 03=重度
  - LDAC：`4d <00/01>` 关/开
  - 查询：`fa 09`（降噪）`fa 4d`（LDAC）`fa 0c`（电量）

## 注意事项

- **BLE 控制通道为单连接**：电脑连接时，手机 App 需先断开；反之亦然
- **切换 LDAC 会短暂断连**，且把降噪重置为"正常"，本工具会自动重连并恢复记忆的降噪档位
- 电量字节即百分比（已用日志满电样本验证 `0x64`=100%）

## 环境需求

- 运行 exe：Windows 10/11
- 源码运行：Python 3.9+，`pip install bleak pystray pillow`